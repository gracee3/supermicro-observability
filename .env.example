# Variable names only. Do not copy this file unchanged into a deployment.
# Let scripts/monitoring-mode create .env securely, or replace the value below
# with a freshly generated random secret and keep the resulting file mode 0600.
GRAFANA_ADMIN_PASSWORD=REPLACE_WITH_A_RANDOM_64_HEX_CHARACTER_VALUE
OBSERVABILITY_UID=1000
OBSERVABILITY_GID=1000
